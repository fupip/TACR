# 测试数据集评估功能说明

## 📋 功能概述

为 `simple_test.py` 添加了对训练集和测试集轨迹数据的双重评估功能，能够全面评估模型的拟合能力和泛化能力。

## 🆕 新增功能

### 1. 双轨迹数据加载
```python
# 加载训练轨迹数据
train_dataset_path = f'trajectory/{args.dataset}_train_traj.pkl'
train_trajectories = load_trajectory_data(train_dataset_path)

# 加载测试轨迹数据  
test_dataset_path = f'trajectory/{args.dataset}_test_traj.pkl'
test_trajectories = load_trajectory_data(test_dataset_path)
```

### 2. 性能对比分析函数
```python
def compare_train_test_performance(train_results, test_results, dataset_name):
    """比较训练集和测试集的性能差异"""
```
- 计算准确率差异和比率
- 泛化能力评估（优秀/良好/一般/需要改进）
- 过拟合检测和建议
- 预测分布分析

### 3. 详细评估报告函数
```python
def generate_detailed_evaluation_report(train_results, test_results, dataset_name):
    """生成详细的评估报告，包括混淆矩阵和分类报告"""
```
- 训练集和测试集的分类报告
- 混淆矩阵可视化
- 各类别性能分析
- 性能差异统计

## 🔄 评估流程

### 完整评估流程
```
训练轨迹数据 → 模型训练 → 训练集评估
    ↓
测试轨迹数据 → 测试集评估 → 性能对比分析 → 详细报告生成
```

### 数据加载策略
1. **优先加载新格式**：`{dataset}_train_traj.pkl` 和 `{dataset}_test_traj.pkl`
2. **兼容旧格式**：如果新格式不存在，自动回退到 `{dataset}_traj.pkl`
3. **智能提示**：建议用户运行 `create_data.py` 生成测试轨迹

## 📊 评估输出

### 1. 训练集评估
```
============================================================
在训练数据集上评估模型
============================================================
正在评估模型的动作预测能力...
使用轨迹中的动作作为ground truth
动作预测准确率: 0.7234
训练数据集准确率: 0.7234
```

### 2. 测试集评估
```
============================================================
在测试数据集上评估模型
============================================================
正在评估模型的动作预测能力...
使用轨迹中的动作作为ground truth
动作预测准确率: 0.6556
测试数据集准确率: 0.6556
```

### 3. 性能对比分析
```
============================================================
数据集: csi - 性能对比分析
============================================================
训练集准确率: 0.7234
测试集准确率: 0.6556
准确率差异: -0.0678
准确率比率: 0.9063
泛化能力评估: 优秀

预测分布分析:
训练集预测分布: [45 45]
测试集预测分布: [42 48]
```

### 4. 详细评估报告
```
============================================================
数据集: csi - 详细评估报告
============================================================

📊 训练集详细报告:
----------------------------------------
分类报告:
              precision    recall  f1-score   support

       Sell       0.72      0.73      0.72        45
        Buy       0.73      0.72      0.72        45

    accuracy                           0.72        90
   macro avg       0.72      0.72      0.72        90
weighted avg       0.72      0.72      0.72        90

混淆矩阵:
[[33 12]
 [12 33]]

📊 测试集详细报告:
----------------------------------------
分类报告:
              precision    recall  f1-score   support

       Sell       0.67      0.69      0.68        42
        Buy       0.64      0.62      0.63        48

    accuracy                           0.66        90
   macro avg       0.66      0.66      0.66        90
weighted avg       0.66      0.66      0.66        90

混淆矩阵:
[[29 13]
 [18 30]]

📈 各类别性能分析:
----------------------------------------
Sell:
  训练集准确率: 0.7333
  测试集准确率: 0.6905
  性能差异: -0.0428
Buy:
  训练集准确率: 0.7333
  测试集准确率: 0.6250
  性能差异: -0.1083
```

## 🎯 关键特性

### 1. 智能数据加载
- 自动检测轨迹文件格式
- 向后兼容旧格式
- 清晰的错误提示和建议

### 2. 全面性能评估
- 训练集拟合能力
- 测试集泛化能力
- 过拟合检测
- 类别级别性能分析

### 3. 结果保存
- JSON格式保存所有评估结果
- 包含原始预测和真实标签
- 性能对比分析结果
- 详细评估报告数据

## 🚀 使用方法

### 基本运行
```bash
python simple_test.py --dataset csi --epochs 50
```

### 预期输出文件
- `results_csi_evaluation.json`：完整评估结果
- `results_csi_train_only.json`：仅训练结果（如果没有测试轨迹）

## ⚙️ 配置要求

### 轨迹文件格式
- **训练轨迹**：`trajectory/{dataset}_train_traj.pkl`
- **测试轨迹**：`trajectory/{dataset}_test_traj.pkl`
- **兼容格式**：`trajectory/{dataset}_traj.pkl`

### 依赖库
- `scikit-learn`：用于生成详细评估报告
- `numpy`：数值计算
- `torch`：深度学习框架

## 🔍 故障排除

### 常见问题
1. **测试轨迹不存在**：运行 `create_data.py` 生成
2. **scikit-learn未安装**：`pip install scikit-learn`
3. **轨迹文件损坏**：检查文件完整性

### 调试建议
1. 检查轨迹文件是否存在
2. 验证轨迹数据格式
3. 确认依赖库安装
4. 查看详细错误日志

---

*功能更新时间: 2024年*
*主要改进: 支持双轨迹评估、性能对比分析、详细报告生成*
